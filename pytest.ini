[pytest]
addopts = -ra
markers =
    integration: integration tests
    db: database lifecycle tests
    extraction: model extraction & summary tests
    fts: full-text search tests
    webui: FastAPI web UI endpoint tests
    fileops: file operations (sync/async)
    thumbnails: thumbnail generation & fallbacks
    launch: webui launch wrapper tests
testpaths = test_integration
